# Railway Configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn api_server_light:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
